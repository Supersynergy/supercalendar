# Turso / libSQL — required for persistent events on Cloudflare.
# Local dev without these falls back to a file store (data/events.db).
# DATABASE_URL="libsql://your-db.turso.io"
# DATABASE_AUTH_TOKEN="your-turso-token"
